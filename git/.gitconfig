[user]
	name = Stan
	# email =
[merge]
	tool = nvimdiff
[diff]
	tool = nvimdiff
[difftool "nvimdiff"]
	cmd = nvim -d "$LOCAL" "$REMOTE"
[difftool]
	trustExitCode = true
[mergetool]
	trustExitCode = true
